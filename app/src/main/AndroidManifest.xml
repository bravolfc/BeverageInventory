<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ishwal.beverageinventory">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".MasterApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".ui.changepassword.ChangePasswordActivity"></activity>
        <activity android:name=".ui.setting.SettingActivity" />
        <activity android:name=".ui.auth.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.inventory.InventoryActivity" />
        <activity android:name=".ui.addinventory.AddInventoryItemActivity" />
        <activity android:name=".utils.scanner.ScannerActivity" />
        <activity android:name=".ui.dashboard.DashboardActivity" />
        <activity android:name=".ui.recovery.ForgotPasswordActivity" />
        <activity android:name=".ui.auth.signup.SignUpActivity" />
    </application>

</manifest>